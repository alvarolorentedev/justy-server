# import SimpleResponse from "simpleResponse.graphql"

type Query {
    empty: String
}

type Mutation {
    createBuyer(email: String!, password: String!): SimpleResponse!
    validateCredentialsBuyer(email: String!, password: String!): SimpleResponse!
    createBidder(email: String!, password: String!): SimpleResponse!
}